<form method="post" action="{{ path('loto_delete', {'id': loto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loto.id) }}">
    <button class="button is-danger">Supprimer</button>
</form>
