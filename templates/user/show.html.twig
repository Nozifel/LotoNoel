{% extends 'base.html.twig' %}

{% block title %}Utilisateur fiche{% endblock %}

{% block body %}
<section class="section">
    <div class="container">
        <div class="columns is-multiline">
            <div class="column is-full">
                <h1 class="title">Utilisateur</h1>
            </div>

            <div class="column is-full">
                {{ include('user/card.html.twig') }}
            </div>

            <div class="column is-full">
                <a href="{{ path('user_index') }}" class="button">Retour à la liste</a>
            </div>

            <div class="column is-full">
                <a href="{{ path('user_edit', {'id': user.id}) }}" class="button is-link">Modifier</a>
            </div>

            <div class="column is-full">
                {{ include('user/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</section>
{% endblock %}
